# 手动测试方案 — v2.4.1 回归 + 全面验证

> **背景**：v2.4.1 修复了 9 个 UX/UI 问题（仪表盘美化、文档详情布局、删除崩溃、模型切换、对话 Markdown+引用、测评历史等）。本方案针对修复点做回归验证，同时覆盖高风险边界场景。

---

## 前置准备

- [ ] `python backend/app.py` 启动成功，日志输出 v2.4
- [ ] 浏览器打开 http://127.0.0.1:5000
- [ ] 准备 1 份测试 MD 文件（200+ 字）
- [ ] 需已有账户，或注册一个新的 `tester241`

---

## 第一部分：修复点回归验证（9 项）

### FX-01 仪表盘视觉升级

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 登录后进入仪表盘 | 顶部横幅有标题"学习仪表盘"+ 3 个快捷统计 |
| 2 | 观察指标卡片 | 4 张卡片各有一条顶部彩色边（绿/橙/金/紫） |
| 3 | 查看热力图 | 有图例说明（白=0 / 浅粉=少 / 中粉=中 / 橙=多 / 深棕=专注），格子有圆角和间隙 |
| 4 | 查看趋势图 | 柱状图代替原来的折线，柱子有渐变橙色 + 顶部显示分钟数标签 |
| 5 | 空数据状态 | 若为新用户无数据 — 热力图和趋势图应不报错、不崩溃 |

**实际结果**：`[    ]`

---

### FX-02 文档详情改为上下布局

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 上传资料后点击"查看" | 展开详情面板 |
| 2 | 检查元数据区域 | 每个字段是**标签在上、值在下**（如"状态"→绿色徽章、"大小"→XX KB） |
| 3 | 关闭详情 | 点击"关闭"按钮，面板隐藏 |

**实际结果**：`[    ]`

---

### FX-03 删除文档 → 不崩溃

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在资料库中点击某文档的"删除" | 弹出确认框 |
| 2 | 点击确认 | 文档从列表消失（可看到短暂淡出），不会出现空白页或崩溃 |
| 3 | 刷新页面 | 仍能正常显示资料库列表，无 JS 错误 |
| 4 | 若有详情面板已打开 | 删除后面板自动关闭 |

**实际结果**：`[    ]`

---

### FX-04 仪表盘与进度页不再重复

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 登录后看仪表盘 | 仪表盘重点是热力图 + 趋势 + 指标 |
| 2 | 切换到"进度"页 | 进度页重点是五级掌握度 + 知识点列表 |
| 3 | 对比 | 仪表盘不再出现"薄弱知识点"列表（已精简） |

**实际结果**：`[    ]`

---

### FX-05 设置页模型切换（选中即切换）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 进入"设置"→ 模型选择 | 只有下拉框，**没有"切换模型"按钮** |
| 2 | 下拉框中选择另一个模型 | 立即触发切换，顶部出现"已切换至 XXX"的绿色提示 |
| 3 | 切换到对话页 | 对话页顶部"模型"下拉框也同步更新 |

**实际结果**：`[    ]`

---

### FX-06 对话页模型切换下拉框

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 进入对话页 | 顶部"关联文档"右边有"模型"下拉框（不是只读文字） |
| 2 | 选择另一个模型 | 触发切换，提示"已切换至 XXX" |
| 3 | 立即发消息 | 回复由新模型生成 |

**实际结果**：`[    ]`

---

### FX-07 对话 Markdown 渲染 + 引用来源

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 确保已有已解析文档，进入对话，选择该文档 |
| 2 | 提问："请列出本文的 3 个核心概念，用加粗标注，每个后面加简要解释" | 回复中核心概念**加粗显示**（橙色字体） |
| 3 | 提问："请用列表格式总结本文大纲" | 回复中出现带圆点的 `<li>` 列表，不是纯文字 |
| 4 | 提问任意与文档相关的问题 | 回复下方出现"📎 引用 N 个资料片段"区域 |
| 5 | 点击某个引用卡片 | 卡片悬停变色，点击跳转（或给出提示） |
| 6 | 无文档场景提问 | 回复无引用来源区域（正常），但 Markdown 仍然渲染 |

**实际结果**：`[    ]`

---

### FX-08 删除对话 → 快速响应

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 对话页左侧列表，点击某对话的 × 按钮 | 确认弹窗 |
| 2 | 点击确定 | 该对话立即从列表中消失（不再长时间等待） |
| 3 | 若删除的是当前打开对话 | 主对话区显示"对话已删除，开始新的对话吧" |
| 4 | 右侧列表刷新 | 其余对话正常显示 |

**实际结果**：`[    ]`

---

### FX-09 测评题量可选更多 + 历史可点击

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 进入测评，看题目数量下拉 | 可选 4/6/8/10/12/15/20 题（7 个选项） |
| 2 | 完成一次测评（任选题量） | 提交后正常显示分数和答题详情 |
| 3 | 向下滚动到测评历史 | 每条历史右侧有"点击查看详情" |
| 4 | 点击某条历史 | 展开面板，显示每道题的标题、你的答案、正确答案、反馈、知识点标签 |
| 5 | 再次点击同一条 | 面板收起 |

**实际结果**：`[    ]`

---

## 第二部分：高风险边界场景

### ED-01 极快连续操作删除

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 上传 3 份文档 | 全部解析完成 |
| 2 | 连续快速删除 2 份 | 两次操作均生效，列表正确，无崩溃、无空白页 |
| 3 | 保留 1 份文档 | 该文档仍可正常查看/对话 |

**实际结果**：`[    ]`

---

### ED-02 空数据下的页面遍历

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 创建一个全新账户 | 无文档、无对话、无测评 |
| 2 | 依次打开 8 个标签页 | 每个页面都显示友好的空状态提示，不报错、不崩溃 |
| 3 | 在仪表盘 | 热力图和趋势图为空但不报错 |

**实际结果**：`[    ]`

---

### ED-03 对话页模型切换后立即提问

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 进入对话，切换模型 | 切换成功提示出现 |
| 2 | 立即输入问题并发送 | 不要等任何延迟 |
| 3 | 观察回复 | 应收到正常 AI 回复，不应出现"模型未找到"或超时错误 |

**实际结果**：`[    ]`

---

### ED-04 流式回复中途切换页面

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在对话中输入一个较复杂的问题 | AI 开始流式逐字输出 |
| 2 | 不等回复完成，点击"仪表盘"标签 | 页面正常切换，不报错、不崩溃 |
| 3 | 切回对话页 | 之前的对话还在（已完成或仍在生成） |

**实际结果**：`[    ]`

---

### ED-05 测评中途关闭/切换页面

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 开始一次测评，等到题目加载 | 答题页面正常显示 |
| 2 | 不等填完，切换到"资料库" | 页面正常切换 |
| 3 | 切回测评 | 测评页回到初始状态（题目丢失是预期行为） |

**实际结果**：`[    ]`

---

### ED-06 学习计划打卡反复勾选

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 生成一个学习计划 | 进入时间轴 |
| 2 | 勾选 Day 1"已完成" | 进度条更新，复选框选中 |
| 3 | 取消勾选 | 进度条回退 |
| 4 | 再次勾选 | 进度条正确更新 |

**实际结果**：`[    ]`

---

### ED-07 浏览器刷新后的状态保持

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在对话页进行一个正在生成的对话 | 至少有一条已完成消息 |
| 2 | 按 F5 刷新页面 | 回到仪表盘（默认首页） |
| 3 | 切回对话页 | 对话列表仍显示历史对话 |

**实际结果**：`[    ]`

---

### ED-08 窄屏响应式适配

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 浏览器缩窄到宽度约 400px（模拟手机） | 顶栏标签塌陷为可滚动横条 |
| 2 | 仪表盘指标卡 | 改为单列或双列显示，不错位 |
| 3 | 对话页 | 左侧对话栏消失或悬浮，主区正常 |

**实际结果**：`[    ]`

---

## 第三部分：跨模块集成测试

### IT-01 文档→对话→测评 全闭环

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 上传文档 → 等解析 | → 资料库显示"已解析" |
| 2 | 切换到对话 → 选择文档 → 提问 | → AI 回复附带引用来源 |
| 3 | 切换到测评 → 选择文档 → 出题 8 道 | → 题目生成成功 |
| 4 | 完成答题 → 提交 | → 显示分数和逐题反馈 |
| 5 | 切换到进度 | → 知识点掌握度有更新 |

**实际结果**：`[    ]`

---

### IT-02 删除文档后关联对话仍正常

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 上传文档 → 在对话中选择该文档 → 提问 2 次 | 对话历史中有 2 条 AI 回复 |
| 2 | 到资料库中删除该文档 | 删除成功 |
| 3 | 回到对话页 | 对话历史仍存在，但再次选该文档的选项已消失 |

**实际结果**：`[    ]`

---

### IT-03 切换模型后全局生效

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 设置页切到模型 A | 提示成功 |
| 2 | 到对话页 | 模型显示为 A |
| 3 | 提问 | 回复用模型 A |
| 4 | 到评估 → 出题 | 出题用模型 A |

**实际结果**：`[    ]`

---

## 测试总结

### 通过率统计

| 部分 | 用例数 | 通过 | 失败 |
|------|--------|------|------|
| 第一部分：修复回归 | 9 | | |
| 第二部分：边界场景 | 8 | | |
| 第三部分：跨模块集成 | 3 | | |
| **合计** | **20** | **/** | **/** |

### 发现的缺陷

| # | 严重度 | 模块 | 描述 |
|---|--------|------|------|
| | | | |

### 总体评价

- 完成测试用时：约 _____ 分钟
- 整体稳定性： □ A | □ B | □ C | □ D
- **备注**：

> 测试人：________ 日期：________
